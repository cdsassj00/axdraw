{
  "name": "axdraw",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A hand-drawn style virtual whiteboard — Excalidraw-like, with rough-sketch shape recognition.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && npx wrangler deploy",
    "test:e2e": "npm run build && node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
